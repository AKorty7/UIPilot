namespace UIPilot.Editor.Modules.Health
{
    // One per rule. The window lists them in this order.
    internal enum HealthCheck
    {
        ClickEvents,
        EventSystem,
        Raycasters,
        BlockedButtons,
        GamepadNavigation,
        TextSize
    }
}
